[project]
name = "qwenBot"
version = "0.1.0"
description = "QQ furry bot based on NoneBot architecture."
readme = "README.md"
requires-python = ">=3.8, <4.0"

[tool.nonebot]
adapters = [
    { name = "Satori", module_name = "nonebot.adapters.satori" }
]
plugins = []
plugin_dirs = ["qwenBot/plugins"]

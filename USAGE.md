# QwenBotQ 命令使用文档

本文档汇总 QwenBotQ 所有可用指令，按功能模块分类。多数指令也可在群内发送 `帮助` 查看（超管在私聊中发送 `帮助` 会额外显示管理命令与撤回图片命令）。

---

## 通用约定

- 在群聊中召唤 AI 对话需 **@机器人**；私聊则直接发送即可。
- `<参数>` 表示必填，`[参数]` 表示可选。
- 群会话 ID 写作 `g{群号}`，私聊会话 ID 写作 `u{QQ号}`。
- 带 `!` 前缀的命令为超级管理员/管理命令，不在普通群成员的帮助菜单中展示。

---

## 帮助

| 指令 | 说明 |
| --- | --- |
| `帮助` | 查看命令列表。仅在超管私聊时额外展示管理与撤回图片命令。 |

---

## 用户系统

| 指令 | 说明 |
| --- | --- |
| `签到` | 每日签到领取积分。 |
| `用户信息 [@用户]` | 查看自己或指定用户的积分、签到状态、模型、今日绑定等信息。 |
| `转账给 @用户 <积分数量>` | 向指定用户转账积分（不允许负数或转给自己）。 |

---

## 绑定系统

| 指令 | 说明 |
| --- | --- |
| `今日老公` | 随机绑定一名今日老公（群聊）。 |
| `换老公` | 解除当前绑定，消耗一定积分。 |
| `续期 [天数]` | 续期当前绑定关系，按天消耗积分，默认 1 天。 |
| `申请绑定 @用户` | 向指定用户申请绑定；对方若已向你申请则会直接建立绑定。 |

---

## 抽奖

| 指令 | 说明 |
| --- | --- |
| `购买奖号 <6位数字>` | 花费积分购买当期奖号，6 位数字且全局唯一，每期每用户限购一个。 |
| `我的奖号` | 查看当前待开奖的奖号。 |
| `!forcedraw` | 强制立即开奖（管理员）。 |

> 每日中午 12:00 自动开奖，按号码匹配位数发放积分奖励。

---

## AI 助手

| 指令 | 说明 |
| --- | --- |
| `@我 <消息>` | 与 AI 对话（群聊需 @，私聊直接发送）。支持图片输入（需当前模型支持图片）、回复引用 markdown。 |
| `使用智能体 <名称>` | 切换本会话智能体（`DEFAULT` / `UNSAFE` 或自定义名称；群聊仅群管/群主，unsafe 仅私聊可用）。 |
| `更改模型 [模型ID]` | 切换 AI 模型，不指定时列出可用模型。 |
| `会话信息` | 查看当前会话 ID、会话智能体与 VIP 到期时间。 |
| `添加智能体 <名称> <提示词> [选项]` | 创建自定义智能体（不可使用保留名）。 |
| `修改智能体 <名称> [选项]` | 修改自定义智能体属性（创建者或超管）。 |
| `删除智能体 <名称>` | 删除自定义智能体（创建者或超管）。 |
| `清空上下文` | 清空当前会话上下文（群聊仅群管/群主）。 |
| `!renewvip <会话ID> <天数>` | 为指定会话续期 AI VIP（仅超管）。 |

**添加/修改智能体选项：**

- `-t / --temperature <值>` —— 温度参数（默认 1.0）
- `-f / --frequency_penalty <值>` —— 频率惩罚（默认 0.0）
- `-p / --presence_penalty <值>` —— 存在惩罚（默认 0.0）
- `-m / --max_tokens <值>` —— 最大输出长度
- `-T / --thinking` —— 是否开启思考模式（默认 False）
- `-u / --unsafe <值>` —— 标记为 unsafe（仅私聊会话可使用，默认 False）
- 修改可用 `--prompt <提示词>` 更新系统提示词。

---

## 图片搜索（EHentai）

| 指令 | 说明 |
| --- | --- |
| `找本子 [图片]` | 以图搜本。 |
| `下本子 <URL>` | 下载本子并生成带密码的 7z 压缩包，上传到当前会话。 |
| `搜本子 <关键词> [-l 数量] [-e]` | 按关键词搜索本子，`-l` 控制展示条数，`-e` 切换 ExHentai。 |
| `下一页` | 查看搜索结果下一页（会记住当前会话的搜索状态）。 |

---

## 管理命令（群管/群主 或 超级管理员）

> 需机器人具备群主或管理员权限。`@用户` 也可替换为 `QQ号` 或回复其消息。

| 指令 | 说明 |
| --- | --- |
| `禁言 @用户 [原因] [-d 时长]` | 禁言用户，默认 1 小时；时长写 `0s` 即解除禁言。 |
| `踢出 @用户 [原因] [-b]` | 踢出用户，`-b` 同时封禁（拒绝再次加入）。 |
| `处理申请 approve\|reject [原因]` | 处理加群申请（需回复申请消息）。 |
| `撤回消息` | 撤回本身；若回复了某条消息则一并撤回那条消息。 |

---

## 撤回图片（仅超级管理员私聊）

> 仅对监听会话中的图片生效；图片缓存保留 5 分钟，被撤回的图片会转移到独立目录保存。

| 指令 | 说明 |
| --- | --- |
| `!listenrecalls <sessionId>` | 添加监听撤回图片的会话（群 `g{群号}` / 私聊 `u{QQ号}`）。 |
| `!listlistening` | 查看当前监听会话。 |
| `!dellistening <sessionId>` | 移除指定监听会话。 |
| `!getrecalls` | 获取已读偏移之后的新撤回图片（逐张内联发送）。 |
| `!getrecallszip` | 将已读偏移之后的新撤回图片打包为 zip 上传。 |
| `!setrecalloffset <偏移>` | 手动指定已读偏移。 |
| `!delrecalls` | 删除当前保存的所有已撤回图片并重置已读偏移为 0。 |

---

## 日志告警（自动）

机器人运行期间产生的 `WARNING` 及以上级别日志会自动转发到所有超管的私聊，格式为 `[日志告警] <级别> | <模块>\n<消息>`。同一条 `(模块, 消息)` 在 5 秒内仅发送一次以避免刷屏；机器人未连接或转发失败时静默跳过，不影响运行。该行为不可关闭，如需停用请在 `qwenbotq/__init__.py` 中注释 `import_module(".lognotice", __package__)`。